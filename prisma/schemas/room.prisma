model Room {
    id        Int      @id @default(autoincrement())
    createdAt DateTime @default(now())
    updateAt  DateTime @updatedAt
    messages  Message[]
    users     User[]   @relation("UserRooms", references: [id])
}
